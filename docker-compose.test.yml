version: '3.8'

services:
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    volumes:
      - .:/app
    command: pytest -v
    environment:
      - PYTHONPATH=/app
      - TESTING=true 